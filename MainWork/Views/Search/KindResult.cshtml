@{
    if (ViewBag.ajax != null)
    {
        @(Layout = null);

    }
}
<script>
    //var y;
    //if (y == null) {
    //    y = 0;
    //}
    history.state.kindId = '@ViewBag.kindId';
    history.replaceState(history.state, null);
    $.ajax({
        beforeSend:function() {
            $(".mainbox").html(
                `<div class="loading">
                <i class="fa fa-circle-o-notch fa-spin fa-3x fa-fw"></i>
                <span class="sr-only">Loading...</span>
            </div>`);
        },
        url: '@Url.Action("KindResultView", "Search")',
        data: history.state,
        type: "post",
        success: function (data) {
            $(".mainbox").html(data);
        },
        complete: function () {
            $(window).scrollTop(y);
        }
    })
</script>

